import React, { useState } from 'react';
import Styles from '../Styles/HashTagView.css';

const HashTagView = () => {
    return(
        <div>
            <h1 className="title">Filter by hashtag</h1>
        </div>
    );
};

export default HashTagView;